<nav class="navbar navbar-expand-lg navbar-dark bg-dark mb-4">
    <div class="container-fluid">
        <a class="navbar-brand" href="/">Small Jira</a>
        <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar" aria-controls="mainNavbar" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
        </button>

        <div class="collapse navbar-collapse" id="mainNavbar">
            {% if is_granted('ROLE_ADMIN') %}
                <ul class="navbar-nav me-auto mb-2 mb-lg-0">
                    <li class="nav-item">
                        <a class="{{ app.request.get('_route') == 'app_task_dashboard' ? 'nav-link active' : 'nav-link' }}" href="{{ path('app_task_dashboard') }}">Tablica</a>
                    </li>
                    {# <li class="nav-item">
                        <a class="{{ app.request.get('_route') == 'app_tasks' ? 'nav-link active' : 'nav-link' }}" href="{{ path('app_tasks_dashboard') }}">Zadania</a>
                    </li> #}
                </ul>
            {% endif %}
            <ul class="navbar-nav ms-auto">
                {% if app.user %}
                    <li class="nav-item">
                        <a class="nav-link" href="#">Zalogowano jako {{ app.user.email }}</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="{{ path('app_logout') }}">Wyloguj</a>
                    </li>
                {% else %}
                    <li class="nav-item">
                        <a class="{{ app.request.get('_route') == 'app_login' ? 'nav-link active' : 'nav-link' }}" href="{{ path('app_login') }}">Zaloguj</a>
                    </li>
                {% endif %}
            </ul>
        </div>
    </div>
</nav>
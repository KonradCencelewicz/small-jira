{% extends 'base.html.twig' %}

{% block title %}Dashboard{% endblock %}

{% block body %}
<div class="container-fluid py-4">
    <div class="row" id="task-board">
        {% for status in statuses %}
            <p>{{ status.label }}</p>
        {% endfor %}
        {% for task in tasks %}
            <p>{{ task.status.label }}</p>
        {% endfor %}
        {# {% for status, tasks in tasksByStatus %}
            <div class="col-md-4">
                <h4 class="text-center text-capitalize">{{ status }}</h4>
                <div class="card bg-light mb-3">
                    <div class="card-body p-2 task-column" id="column-{{ status }}" data-status="{{ status }}">
                        {% for task in tasks %}
                            <div class="card mb-2 task-card" data-task-id="{{ task.id }}">
                                <div class="card-body p-2">
                                    <h5 class="card-title">{{ task.title }}</h5>
                                    <p class="card-text small text-muted">{{ task.description }}</p>
                                </div>
                            </div>
                        {% endfor %}
                    </div>
                </div>
            </div>
        {% endfor %} #}
    </div>
</div>

<a href="{{ path('app_task_create') }}"
   class="btn btn-primary btn-lg rounded-circle"
   style="position: fixed; bottom: 20px; right: 20px; width: 60px; height: 60px; display: flex; align-items: center; justify-content: center; font-size: 24px;">
    +
</a>
{% endblock %}
